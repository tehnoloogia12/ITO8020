create table inimesed(
    id integer primary key,
    nimi varchar(100),
    palk integer,
    sugu varchar(1)
);

create table autod (
    id integer primary key,
    mark varchar(100),
    numbet varchar(10),
    omanik integer
);